一、我为什么要做一套「skills」

1. 根本目的
- 我希望在「用 AI 写代码 / 做设计 / 帮忙思考」的时候，不是每次临时堆一大段 prompt。
- 我想要一套可以复用、可组合的「skills」，像一个个人工具箱，让 AI 在不同项目里都能稳定符合我的风格和习惯。
- 这些 skills 不是面向最终用户的功能，而是用来约束 / 引导 AI 行为的“能力模块”和“人格设定”。

2. 使用场景（不限于 UI）
- 设计前端 UI（配色、布局、组件风格等）。
- 写业务代码（比如接口层、状态管理、错误处理习惯）。
- 代码结构与命名（如何拆组件、文件组织、命名规范）。
- 甚至可以扩展到文案风格、接口设计风格、测试习惯等。


二、我心里对「skill」的定义

1. skill 是什么
- 一个 skill = 一块可以单独拿出来复用的「行为/风格/能力配置」。
- 它不是具体某一页的详细实现，而是：
  - 给 AI 的一组原则、偏好和禁用项；
  - 有明确边界：负责哪一块、做什么、不做什么。

2. skill 可以包含什么
- 文本说明（给 AI 看的规则、偏好、禁用项）。
- 可选的配置项（比如：主题主色、密度偏好、语言风格等）。
- 将来还可以扩展：脚本、模板、示例代码、素材等，但这些是加分项，不是现在的重点。

3. skill 不是什么
- 不是某一个具体页面的像素级描述。
- 不是一次性的临时 prompt。
- 不是一坨什么都往里塞的大杂烩；它应该职责单一，可清晰命名。


三、我希望「skills」整体能做到什么

1. 行为稳定
- 同样的 skill 组合，在不同时间、不同项目里使用，AI 的行为和风格应该尽量稳定，不会忽冷忽热乱变。
- 比如：启用了某个“极简商务 UI” skill 后，不管当前业务是什么，AI 设计出来的界面都应保持极简、低饱和、无 emoji、无夸张阴影等特征。

2. 快速切换和复用
- 我可以通过简单的话语选择 skill 组合：
  - 例如：「启用 XXX 基础风格 + YYY 主题 + ZZZ 平台习惯」。
- 这样在换项目、换端（PC / 小程序 / H5）时，不用重新从零说清所有偏好，只要换一组 skills。

3. 把“我的品味和习惯”固化下来
- skills 里记录的是「我认同的美学、代码习惯、结构偏好」，随着时间可以迭代。
- 新项目、新需求只是在这些基础上做加法，而不是推翻重来。


四、我对「skills 设计方式」的要求（通用，而不局限于 UI）

1. 抽象层级
- 更多描述：
  - 气质 / 风格（例如：极简、克制、偏工具、偏理性……）。
  - 禁用项（坚决不要的东西，比如高饱和渐变、emoji 图标、随意命名）。
  - 结构/思路倾向（例如：先理清数据结构再写组件；先布局再细化视觉）。
- 尽量少写死具体的实现细节：
  - 少写具体像素、精确色值、某个函数的完整实现。
  - 避免把 skill 绑死在某一个页面或某一个业务上。

2. 模块化、单一职责
- 一个 skill 只管一件相对单一的事情：
  - 审美基因是一类。
  - UI 主题风格是一类。
  - 平台/端的布局习惯是一类。
  - 代码结构习惯又是另一类。
- 这样我在用的时候，可以按需选用、组合，而不会互相打架。

3. 有明确的「硬约束」和「软偏好」
- 硬约束：绝对不能违反的底线（比如：不要高饱和渐变、不要 emoji 图标、不要随便改后端接口约定）。
- 软偏好：在不影响业务需求的前提下，尽量这样做（比如：倾向用某种布局、倾向用某种命名方式）。
- 在 skill 文本里要能看出来哪些是底线，哪些是建议。

4. 与项目解耦
- skills 本身尽量通用，不写业务名、公司名、具体产品名。
- 某个具体项目需要特殊规则时，通过“项目层”的 skill 轻量补充，而不是篡改所有基础 skills。


五、目前我们讨论过的，只是「skills 的一个应用方向：UI 设计」

- 之前围绕个人财务管理系统的首页，我们一起摸索了一套：
  - 极简商务的通用 UI 基因；
  - 柔和蓝灰、顶部导航 + Hero + 卡片 + 表格的首页长相；
- 这些内容都属于「UI 主题相关的 skill 示例」，只是整个 skills 体系中的一个分支。
- 更重要的是：通过这次尝试，我明确了自己对 skills 的整体要求：
  - skills 要抽象、可组合、负责“人格和风格”，而不是写死某个页面；
  - UI 只是其中一种类型，未来还可以有“代码习惯类 skill”“接口设计类 skill”等等。
